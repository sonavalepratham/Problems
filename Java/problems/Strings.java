public class Strings {
    public static void main(String[] args) {
        System.out.print("Hello");
    System.out.println("Hello");
    System.out.printf("Hello %8.2f", 1.314f);
    System.out.format("Hello");
    String str_name="Prathmesh";
    System.out.println(str_name.length());
    /*
    str_name.length(); // returns length of string
    str_name.toLowerCase(); // returns string which has Lowercase characters
    str_name.toUpperCase(); // returns string which has Uppercase characters
    str_name.trim(); // returns string after removing leading & trailing spaces
    str_name.substring(startindx); // returns substring starting from startindex
    str_name.substring(beginIndex, endIndex); // returns substring starting from beginIndex to endIndex-1
    str_name.replace(oldChar, newChar); // replace oldchar by newchar
    str_name.startsWith(prefix); // returns true if starts with prefix
    str_name.endsWith(suffix); // returns true if ends with suffix
    str_name.charAt(index); // returns cha at index
    str_name.indexOf(str); // returns index of 1st occurence of str, -1 otherwise
    str_name.equals(anObject); // returns true if strings are equal
    str_name.equalsIgnoreCase(anotherString); 
    */
    }
}
